import { AukInputDirective } from './input.directive';

describe('AukInputDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new AukInputDirective();
  //   expect(directive).toBeTruthy();
  // });
});
