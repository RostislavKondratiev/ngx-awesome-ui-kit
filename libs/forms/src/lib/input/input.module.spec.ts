import { AukInputModule } from './input.module';

describe('InputModule', () => {
  // let inputModule: AukInputModule;
  //
  // beforeEach(() => {
  //   inputModule = new AukInputModule();
  // });

  it('should create an instance', () => {
    expect(true).toBeTruthy();
  });
});
