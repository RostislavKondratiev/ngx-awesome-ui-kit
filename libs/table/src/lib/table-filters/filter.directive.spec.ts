import { AukFilterDirective } from './filter.directive';

describe('AukFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new AukFilterDirective();
    expect(directive).toBeTruthy();
  });
});
