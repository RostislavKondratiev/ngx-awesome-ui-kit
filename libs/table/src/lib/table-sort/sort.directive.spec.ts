import { AukSortDirective } from './sort.directive';

describe('AukSortDirective', () => {
  it('should create an instance', () => {
    const directive = new AukSortDirective();
    expect(directive).toBeTruthy();
  });
});
